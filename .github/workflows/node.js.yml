# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x, 14.x, 16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - name: install package...
        run: yarn install

      - name: run build...
        run: npm run build

      - name: Deploy
        run: |
          git config --global user.name tungnguyen741
          git config --global user.email tung.nguyen21098@gmail.com
          git remote set-url origin https://${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}@github.com/3D
          npm run deploy
        env:
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
          github_token: ${{ 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 }}
          repository: ${{ github.repository }}
